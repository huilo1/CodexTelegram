## Базовая проверка владельца — 21 сентября 2026

Владелец сообщил об успешной базовой проверке и разрешил commit/push текущей
реализации. Последний автоматический прогон: **95 passed**. Публикация GitHub
release или тега в этот запрос не входит.

## Исправление hooks после обновления — 21 сентября 2026

Плагин `0.2.0+codex.20260921091508`, **95 passed**. Воспроизведено удаление старого cache launcher,
на который ссылался Stop в уже открытом разговоре. Мастер сохраняет bootstrap-файлы
прежних версий, а новые четыре hooks используют стабильный runtime напрямую.
Проверены все четыре события с отсутствующим PLUGIN_ROOT и подменой модулей из cwd.
Старый launcher активного разговора восстановлен и исполняется; установленный новый
SessionStart исполнился с заведомо отсутствующим кэшем.

Runtime-код в этом исправлении не менялся. Загрузка зависимостей для повторной
установки была недоступна из-за сети; действующий runtime сохранён, plugin-only
обновление выполнено локально. App Server hooks/list подтверждает enabled=true,
trustStatus=modified для новых четырёх команд. Владельцу нужно подтвердить их в
/hooks; доверие автоматически не менялось.

## Файлы, краткие отчёты и темы Telega — 21 сентября 2026

Версия: `0.2.0+codex.20260921091036`. **90 passed**, включая sandbox, очередь файлов,
повторы после lost ACK, ACL, ограничения путей, `/full` и независимые worktree-темы.
Skill и plugin manifest валидны. Реальный Codex выбрал запрошенный demo.txt через
новую JSON-схему; реальный MCP объявляет пять инструментов, включая telegram_send_file.

- Файл доставлен в действующую тему CodexTelegram: Telegram подтвердил отдельно
  текст и документ. Исходник тестового файла удалён после постановки в очередь;
  доставка использовала сохранённую копию. Секретные данные для теста не использовались.
- Темы Telega clients/server созданы отдельно, integration переименована без смены
  topic ID. Маршруты хранятся в БД, скрипты Telega не изменялись. Старые отчёты остались
  в прежней теме. Старые MCP-процессы нужно заменить новыми разговорами Codex.
- Крупные вложения копируются и загружаются частями. Проверка файла 64 MiB показала
  менее 8 MiB Python-выделений при копировании. Реальная сетевая загрузка файла
  предельного размера 2000 MiB не выполнялась; проверена доставка малого документа.
- Публичные исходные отчёты остаются в БД, краткие варианты сохраняют выявленные
  проверки/ограничения и ссылку на `/full`. Это извлечение фрагментов, не обещание
  автоматического понимания значимости каждой фразы.
- Runtime и личный plugin cache обновлены через штатный setup.py --update.
  Doctor: ready=true, Telegram/MCP и heartbeat исправны. Аккаунты и история сохранены.

## Подготовка GitHub-беты — 20 сентября 2026

Версия: `0.2.0+codex.20260920151932`. **74 passed**, включая живую проверку
macOS sandbox. Plugin manifest и skill валидны; `git diff --check` без ошибок.

- Новый wizard требует собственные api_id/api_hash пользователя; общие реквизиты
  не поставляются. Проверены повтор неверного ввода, отмена, сохранение 0600 и
  отсутствие api_hash в выводе. Telega не нужна для новой установки.
- Свежая БД начинает с selected; проверены opt-in, all/selected, явные исключения,
  миграция прежней БД с сохранением all и остановка очереди/вопросов отключённого проекта.
- Installer сначала готовит отдельный runtime, переключает его после pip check и
  проверки импортов, сохраняет предыдущую среду. Проверены отказ установки без
  потери активной среды, переключение/откат и запрет второго writer сессии.
- Архив распакован во временную папку, runtime установлен с нуля в путь с пробелами.
  Реальный MCP initialize/tools прошёл; без ключей/аккаунта doctor корректно сообщил
  о незавершённой настройке. Реальная авторизация нового пользователя не выполнялась.
- `setup.py --update` выполнен на действующей установке: авторизация, БД и all
  сохранились; новый runtime запущен, личный marketplace переустановлен без дубля
  hooks. `doctor --json --mcp`: ready=true, Telegram читается, очередь пуста.
- App Server hooks/list: все четыре обработчика остаются trusted в трёх рабочих
  проектах. Новое доверие автоматически не выдавалось. GitHub-каталог local-dev
  зарегистрирован и видит reports как доступный отдельный плагин.
- Исходники и доступная история Git проверены по типовым секретам и точным локальным
  api_id/api_hash/приглашению: совпадений нет. Проверка эвристическая.
- ZIP собирается без локальных данных, истории Git, внутренней памяти и legacy runtime;
  рядом записывается SHA-256. GitHub Actions добавлен, но на GitHub ещё не запускался.

Локальная среда: macOS Apple Silicon, Python 3.14.6, Codex 0.155.1.
Python 3.11 и чистый Mac другого пользователя пока покрыты планом/CI-конфигурацией,
а не фактическим живым прогоном. Следовать `docs/RELEASING_REPORTS.md` перед
объявлением более широкой совместимости. Commit/push/release в этом этапе не выполнены.

# Проверка Telegram Reports — 20 сентября 2026

Исправление MCP и подтверждения вопросов: **62 passed**. Воспроизведена ошибка
реального App Server из Kir: legacy `.mcp.json` передавал `${PLUGIN_ROOT}` буквально,
Python пытался открыть `Kir/${PLUGIN_ROOT}/scripts/run.py`. Предыдущая проверка
прямого launcher не выявляла этого, потому что подставляла путь самостоятельно.
Теперь тест запускает точную команду манифеста из соседней папки с пробелами
и с подставленным PYTHONPATH/typing.py; runtime запускается изолированно через `-I`.

После установки `0.2.0+codex.20260920143024` реальный App Server в Kir подтвердил
MCP handshake и четыре инструмента (`toolsError: null`). Runtime совпадает
с исходниками; hooks проверены и доверены для Kir, Telega и CodexTelegram.

Подтверждения вопросов проверены на повторную доставку, восстановление после
перезапуска, flood wait, отключённые реакции, ACL, отсутствие дубликатов и несколько
частей ответа. Долгая обработка первого вопроса не задержала отметку второго.
Живой Telegram подтвердил обновление read_inbox_max_id нужной темы и собственную
реакцию 👀 на последнем уже сохранённом вопросе владельца. Тестовая реакция после
проверки восстановлена в прежнее состояние. Служба перезапущена, очередь пуста.

После расширения уточнений: **52 passed**, включая живые проверки macOS sandbox.
Добавлены проверка snapshot/MCP, исключение секретных и игнорируемых файлов,
защита от symlink/hardlink/path traversal, diff без внешних команд и связь reply
с исходной задачей/worktree. Проверены реальные запреты записи в проект/снимок,
чтения посторонних файлов, TCP/Unix socket, запуска дочерних программ и сигналов
внешнему тестовому процессу. Разрешены AST-анализ и запись в scratch; лимит вывода
и остановка долгого процесса также проверены.
Git clean/process filters и показ подписей отключены при сборе контекста;
тест с настроенными внешними diff/clean/process командами не запустил их.
Добавлен regression test временной ошибки launchd bootstrap после bootout.

Реальный Codex 0.155.1 вызвал инструменты project_reader, прочёл реализацию
create_server, посчитал определения функций через run_python/AST и ответил
с указанием файлов и строк. `code_mode_host` необходим для вызовов MCP в этой
сборке; общий shell остаётся отключён. Отдельный живой тест guard временно включил
exec в тестовом запуске: `pwd` был заблокирован PreToolUse с новой причиной
`Telegram questions may use only the project_reader snapshot tools`.
Просьба изменить исходник и запустить приложение получила отказ; файл не изменён.

Версия на предыдущем этапе: `0.2.0+codex.20260920141729`. Все 14 Python-модулей runtime
совпадают с исходниками. Четыре hooks включены и доверены для CodexTelegram,
Telega и Kir. Служба перезапущена; итоговый отчёт об обновлении подтверждён Telegram
(message_id 14), очередь пуста, heartbeat свежий. До обновления в группе получены
и отвечены четыре вопроса владельца; все четыре ответа доставлены.

После добавления QR: **43 passed**. Проверены регистрация обработчика до показа
QR, автоматическое обновление, 2FA, ограничение времени, переключение `/qr`,
локальная SVG-страница, права 0600 и удаление изображения токена при завершении.
Пользователь подтвердил успешную авторизацию реального аккаунта по QR.

Повторная проверка после исправления мастера входа: **37 passed**. Добавлены
пустой код без скрытой повторной отправки, неверный/истёкший код, смена hash при
`/resend`, серверный timeout, отсутствие fallback, flood wait, отмена и повтор 2FA.
Доставка кодов реальным Telegram остаётся отдельной проверкой в мастере.

## Выполнено

- `pytest -q --disable-warnings`: **28 passed** (включая прежние тесты legacy-бота).
- Несколько задач в одном проекте; отдельные проекты с одинаковыми именами;
  объединение Git worktrees; повтор события после открытия БД заново.
- ACL владельца, неизвестные темы, дубликаты входящих сообщений, durable cursor.
- Контекст вопросов не включает другой проект или ещё не доставленный отчёт.
- Повтор доставки сохраняет Telegram random_id и topic; reply адресуется вопросу.
- Запрет команд разработки, `/status` без модели, безопасный ответ при сбое модели.
- Разбиение по UTF-16, редактирование типовых секретов, исключение исходного
  пользовательского запроса из автоматического стартового отчёта.
- MCP: реальный stdio initialize, tools/list, report и status; отдельно проверен
  launcher установленной копии плагина в Codex cache.
- Plugin manifest и SKILL.md прошли валидаторы plugin-creator и skill-creator.
- App Server обнаружил четыре hook без ошибок; для установленной версии проверены
  их определения и выставлены только их trusted hashes.
- Реальный `codex exec` 0.155.1 ответил на вопрос по искусственному снимку отчётов.
- Реальный запрос начать разработку и создать тестовый файл получил отказ;
  файл не появился.
- Первоначальная проверка защитного hook (до расширения уточнений): временно включены инструменты
  в изолированном процессе и запрошен безвредный `pwd`. Codex зарегистрировал
  `PreToolUse Blocked` с причиной `Telegram status questions have no tool access`.
  Проверка выполнена без bypass hook trust; в той версии инструменты были выключены.
- Установленный Python runtime сопоставлен с исходниками. Импортированные API
  ID/hash не обнаружены среди tracked/untracked исходников, разрешённых Git.
- Каталог данных 0700, конфигурация 0600; сеанс Telega не копировался.
- Живой onboarding завершён: `doctor` подтверждает аккаунт, группу и владельца;
  launchd-служба работает и обновляет heartbeat.
- По запросу владельца отправлен тестовый отчёт через обычную очередь службы.
  Telegram подтвердил создание темы CodexTelegram (topic_id 3) и отправку
  сообщения (message_id 4). Повторных попыток и ошибок нет, очередь пуста.
  Приглашение для вступления личным аккаунтом передано владельцу.

В pytest есть deprecation warnings pytest-asyncio 0.26 на Python 3.14; проверки
завершились успешно. Это не проверка всех будущих версий Python/Codex/Telegram.

## Ещё не выполнено

Авторизация, создание реальной группы/темы и доставка тестового отчёта подтверждены.
Вступление владельца и четыре обычных ответа подтверждены локальной очередью
с Telegram message IDs. Расширенное чтение/AST проверено живым Codex отдельно;
владелец подтвердил, что новые вопросы по коду через Telegram работают.
Оставшиеся проверки:

1. В новом разговоре Codex запустить обычную задачу. Проверить начало, важный
   промежуточный отчёт и итог в одной теме проекта.
2. Написать `/status`, затем вопрос по коду в ответ на итоговый отчёт. Убедиться, что
   ответ остаётся в той же теме, а основной разговор разработки не изменён.
3. Перезапустить службу при непустой очереди, проверить повторную доставку и отсутствие
   повторного исполнения вопроса. При разрыве Telegram во время ACK отдельно
   проверить поведение сервера с повторным random_id.

Служба рассчитана на текущий Mac. Постоянный сервер и работа при выключенном Mac
в этой версии не предоставляются.
